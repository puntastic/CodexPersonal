# Project work-cue trial catalogue

`project-work-cues.json` is a receiver-side composition of five source-backed
headers. It keeps the existing Muse construction cue unchanged and adds four
Hjarni pointers. This is not a Muse export, a universal correction-card shape,
or an adopted reasoning policy. Hjarni 13190 explicitly preserves native source
ownership and transport plurality.

## Intended use

| Header | Source and useful move |
| --- | --- |
| Finish authorized construction | Existing Muse occurrence; finish the authorized live consequence or name the real blocker. |
| Correction scope | Hjarni 21871 T6/T7; retain the aim and useful parts while repairing the approach actually challenged. |
| Recipient evidence | Hjarni 21248; ask about the part the recipient can observe, without promoting opinion into objective proof. |
| Prospective construction | Hjarni 21871 T7; give a grounded, recoverable possibility a concrete next step before demanding proof of benefit. |
| Capability and authority | Hjarni 21921; check present capability and actual delegation separately, preserving binding restrictions. |

The last source describes a historical Work incident; it does not certify a
current Desktop capability. Two headers use different parts of the same
conversation. They are distinct moves, not independent corroborating evidence.
The source lock versions identify the snapshots used to author the headers;
they do not freeze the sources or prove currentness at later contact.

## Receiver and observation boundary

The runtime selects at most one header using the existing lexical matcher and
two-turn cooldown. A pointer can nominate source contact, not compel adoption.
The source retains its full observation, correction trajectory, positive
examples, uncertainty, and release context. No full note is injected.

These headers are field candidates. The deterministic checks establish loader
acceptance, bounded pointers, and the selections or abstentions in
`../tests/fixtures/project-cue-cases.json`. Those are authored development and
regression cases, not held-out semantic evaluation or proof of usefulness.
Each positive family has a nearby non-use case; ordinary unmatched work remains
quiet. Existing scope/cooldown and assessment integration tests exercise the
runtime around these data checks.

Ordinary work should now supply actual selected episodes and the recipient's
bounded report. Refine a misleading handle or source projection when that
evidence identifies one. Missing lexical coverage, turn-opening-only contact,
and loss of retained request state on restart remain limitations; no semantic
retriever, new transcript store, or compulsory evaluation program is added.

## Install and recover

Copy the checked source file to
`$CODEX_HOME/cue-exports/project-work-cues.json`, and set the existing
`features.cue_activation.catalog_path` to that relative path. Preserve the prior
catalogue and config before replacement, compare installed bytes with source,
then inspect a live decision's catalogue SHA after the receiver reloads.
Keep advisory mode unchanged. Catalogue selection is separate from deploying a
new receiver binary; verify each plane independently.

Hjarni 22434 owns the motivating review; 21188 owns the workstation deployment
lineage. This directory owns the exact composed catalogue and its rationale.
