# Upstream catch-up — 2026-09-13

## Purpose and authority

Kestrel requested a current environment update pass: stay reasonably close to
upstream so hosted Desktop/model/protocol compatibility does not drift into an
unsupported endpoint. Selective parity is the goal, not adopting every upstream
behavior or undoing working local fixes. Ath177 carries the adopted doctrine;
Ath1328 carries the preceding UI/phone investigation. Astra's late reaction to
already-posted input is explicitly not a repair target. Gemini remains parked.

The current task and established CodexPersonal maintenance grant cover source
integration, focused validation, build, publication and reversible next-start
selection. Do not terminate the running Desktop session or perform live database
migrations as a test. A materially different authority/behavior decision stays
with the user. Preserve the current release and scoped data recovery where a
new migration would make binary rollback insufficient.

The user specifically clarified that compatibility must not silently import a
foreign authority model. Full Access must bypass Guardian review before its
lane, model requirements or extension dispatch. Review-denial accounting must
remain lane-local, not terminate the acting turn. Inspect new defaults and
activation paths as well as code; any unavoidable authority change requires a
user decision before next-start selection.

## Exact starting state

- Deployed and personal/main source:620e488dcbbb2613fa4e723a1d6633c10d8a1ad2.
- Prior upstream boundary:83b62a02fab5c0fc797cbc9896c332148f1fd9d0.
- Running package fingerprint:1508c59a12fad5043766a47d5768880a9d068054fc9a8ad0498e64c754943e49.
- Current packaged Desktop:26.908.4834.0; its bundled CLI reports0.154.0-alpha.6.2.
- Initial upstream target:stable0.154.0,6b9826e3aa83b1a5947db50f4332cb9c65f1b340,
  publishedSeptember9. Divergence:34 personal commits /102 upstream commits.
- Observed upstream main:a4c61afff27b954e3c0831c728f11f8f9a17ae73,412 commits
  beyond the prior common boundary; not an automatic integration target.
- Isolated branch:codex/upstream-sync-20260913, worktreeCodex-Desktop-Client-sync-20260913.
- Source checkout and deployment selectors were clean/consistent; no pending
  deployment transaction. About226GiB free at start. No cache cleanup authorized.

## Arc

1. **Bind the update.** Inspect the doctrine, exact deployed source, current
   upstream/ref and running selection. Complete; original checkout/release stay intact.
2. **Integrate stable.** Merge stable0.154.0 into this purpose branch; resolve
   conflicts by retained behavior, not wholesale ours/theirs. Keep explicit
   dispositions where upstream replaces or conflicts with local code.
3. **Qualify seams.** Use upstream-integration.md plus the local retention map:
   PaginatedRefsV1/V2 and exact/digest-bound compaction recovery; independent
   Guardian/retained-authority evidence; migration number/checksum compatibility;
   optional-log fallback and explicit durability barriers; lane-only approval
   breaker; capability/hook routing; disabled cue/context/step-switch experiments;
   Remote compatibility identity and reversible Windows deployment.
4. **Evaluate later fixes.** Review43798/43950/44311/44341 after the stable merge.
   Carry only compatible, useful corrections; no blanket main merge or new GUI
   workaround. Unknown ancestry or semantic overlap is fog to resolve, not permission.
5. **Test and build.** Regenerate affected derived artifacts; use focused just
   recipes and the existing Windows lane, normal dev-small profile and shared
   caches. No all-features or unapproved full workspace suite. Keep source stable
   throughout the build and preserve exact provenance.
6. **Deliver.** Publish/integrate the source through personal, build and smoke
   the canonical package in isolated homes, select for next restart only after
   qualification. Verify source/selected/running states separately. Fresh phone
   use remains the final end-to-end observation, not a source-test claim.

## Fog and stop

The stable merge may expose changed Guardian ownership or persistence contracts.
Inspect and repair those exact joints before selecting a package. If the chosen
boundary cannot preserve a required function within current authority, keep the
candidate and report the specific choice; do not retreat to indefinite freezing
or quietly weaken the function. Stop at a usable compatible integration with
honest test/activation limits, not a new general client redesign.

## Resolution notes (candidate, validation pending)

- Preserve reference-backed checkpoint resolution, rollback correlation and
  deletion-reference completeness alongside upstream's inherited/local retained
  instruction ordering and new thread-owned review context owner.
- Preserve verified V8 cache reuse while applying upstream's pinned checksum
  manifest validation to cached manifests too. The combined 17 packaging tests
  pass; this does not establish the Rust build.
- Accept upstream's removal of `app-server/README.md`. Its only local delta was
  MCP runtime-status documentation, preserved here: `mcpServerStatus/list` keeps
  `runtimeStatus`, `error` and `failureReason` separate from inventory and
  `toolsError`. Startup failures persist until successful connection;
  `reauthenticationRequired` requires retained OAuth credentials. Cached or empty
  tool catalogs do not prove connection, and older servers may omit these fields.
  Protocol code/schema, not this maintenance note, remains the exact API owner.
- A second review found that ThreadOwned context no longer has a legacy review
  backup. A summary-only rollback must therefore discard ambiguous retained
  facts and the opaque model window, rather than silently doing nothing.
- The stable tag stamps workspace crates as 0.154.0 while its checked-in lock
  still lists 0.0.0. Cargo's lock reconciliation changes only those local package
  versions. Do not confuse this source identity with a compatibility override.
- Reuse the existing original checkout's `codex-rs/target` through process-local
  `CARGO_TARGET_DIR`; no junction or duplicate cache is needed. Schema generation
  accepts `--cargo-profile dev-small` so it can reuse the same profile too.
